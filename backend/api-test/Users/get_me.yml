info:
  name: get_me
  type: http
  seq: 1

http:
  method: GET
  url: "{{USERS_URL}}/me"
  headers:
    - name: Authorization
      value: bearer ArifTest@6392
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
