info:
  name: add_plant
  type: http
  seq: 3

http:
  method: POST
  url: "{{PLANTS_URL}}/"
  body:
    type: json
    data: |-
      {
        "name": "Kakrol",
        "description": "Begun Potol",
        "type": "sobji",
        "price": 23.5,
        "stock": 150
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
