info:
  name: get_user_roles
  type: http
  seq: 5

http:
  method: GET
  url: "{{ADMIN_USERS_URL}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
