info:
  name: user_password_reset
  type: http
  seq: 8

http:
  method: POST
  url: "{{ADMIN_USERS_URL}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
