info:
  name: create_user
  type: http
  seq: 1

http:
  method: POST
  url: "{{AUTH_URL}}/register"
  body:
    type: json
    data: |-
      {
        "first_name": "Nadia",
        "last_name": "Karim",
        "email": "rires35516@rpaintel.com",
        "username": "nadia_karim82",
        "password": "NadiaTest@8274"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
