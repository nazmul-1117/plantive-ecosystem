info:
  name: create_user
  type: http
  seq: 1

http:
  method: POST
  url: "{{AUTH_URL}}/signup"
  body:
    type: json
    data: |-
      {
        "first_name": "Md. Nazmul",
        "last_name": "Hossain",
        "email": "nazmul@example.com",
        "username": "nazmul_dev",
        "password": "MyStrongPassword@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
