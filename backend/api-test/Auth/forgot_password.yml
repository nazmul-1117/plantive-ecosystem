info:
  name: forgot_password
  type: http
  seq: 6

http:
  method: POST
  url: "{{AUTH_URL}}/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "rires35516@rpaintel.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
