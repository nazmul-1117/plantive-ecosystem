info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{AUTH_URL}}/login"
  body:
    type: json
    data: |-
      {
        "username": "str1uingxd",
        "password": "nx1d"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
