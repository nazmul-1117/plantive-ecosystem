info:
  name: deactivate_user
  type: http
  seq: 10

http:
  method: POST
  url: "{{ADMIN_USERS_URL}}/activate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
