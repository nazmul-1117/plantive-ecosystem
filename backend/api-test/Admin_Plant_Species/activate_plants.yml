info:
  name: activate_plants
  type: http
  seq: 5

http:
  method: POST
  url: "{{ADMIN_PLANT_SPECIES_URL}}//activate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
