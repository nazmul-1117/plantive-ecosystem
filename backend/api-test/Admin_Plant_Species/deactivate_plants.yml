info:
  name: deactivate_plants
  type: http
  seq: 6

http:
  method: GET
  url: "{{ADMIN_PLANT_CATEGORIES_URL}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
