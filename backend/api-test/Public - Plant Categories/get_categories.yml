info:
  name: get_categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{ADMIN_PLANT_CATEGORIES_URL}}"
  params:
    - name: search
      value: indoor
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
