info:
  name: get_plant_care_guide
  type: http
  seq: 3

http:
  method: GET
  url: "{{PLANT_SPECIES_URL}}/-/care-guide"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
